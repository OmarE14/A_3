<!--Code for the navbar contains ejs if and else statments to make the navbar responsive  and allow the icons to be glowing when the user 
clicks on it-->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/"><img src="\Assets\images\NeverLoseURconnections_logo.png" alt="web logo"> <span> Never Lose UR Connections </span></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <% if(title=="Home") { %>
              <a class="nav-link active" href="/home">
            <% } else {%>
              <a class="nav-link" href="/home">
            <% } %>  
            <i class="fa-solid fa-house-chimney-user"></i> Home</a>
          </li>
          <li class="nav-item">
            <% if(title=="About us") { %>
              <a class="nav-link active" href="/aboutus">
            <% } else {%>
              <a class="nav-link" href="/aboutus">  
            <% } %>       
              <i class="fa-solid fa-circle-info"></i> AboutUs</a>
          </li>
           <li class="nav-item">
            <% if(title== "Current Contacts") { %>
              <a class="nav-link active" href="/bookslist">
            <% } else {%>
              <a class="nav-link" href="/showlist">
            <% } %>  
            <i class="fa-solid fa-address-book"></i> MyContacts</a>
          </li>


          <li class="nav-item">
            <% if(title== "My Contacts") { %>
              <a class="nav-link active" href="/bookslist">
            <% } else {%>
              <a class="nav-link" href="/bookslist">
            <% } %>  
           <i class="fa-solid fa-people-roof"></i> Manage My Contacts</a>
          </li>
          
        </ul>
      </div>
    </div>
  </nav>